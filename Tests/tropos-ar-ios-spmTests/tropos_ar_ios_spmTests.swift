import XCTest
@testable import tropos_ar_ios_spm

final class tropos_ar_ios_spmTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(tropos_ar_ios_spm().text, "Hello, World!")
    }
}
